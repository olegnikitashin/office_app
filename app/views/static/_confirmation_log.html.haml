- recent_audit_items.each do |recent_audit_item|
  .homepage-block.col-md-3
    %h4
      = recent_audit_item.user.full_name
    %p
      .pending-details
        Week starting:
      = recent_audit_item.start_date
    %p
      .pending-details
        Confirmed at:
      = recent_audit_item.end_date || status_label('pending')
    %p
      .pending-details
        Status:
      = status_label(recent_audit_item.status)